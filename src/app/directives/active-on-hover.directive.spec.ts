import { ActiveOnHoverDirective } from './active-on-hover.directive';

describe('ActiveOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
